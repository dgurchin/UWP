<Page x:ConnectionId='1'
    x:Class="Inventory.Views.DishesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Inventory.Controls"
    xmlns:controlsUWP="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:views="using:Inventory.Views"
    xmlns:models="using:Inventory.Models"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="ListHeaderTemplate">
            <StackPanel Margin="6,4,0,0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Блюда" FontSize="18" />
                <TextBlock Text="{Binding}" FontSize="18" Foreground="Gray" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Background="WhiteSmoke"
          BorderBrush="LightGray"
          BorderThickness="1,0,0,0">
            <controls:WindowTitle x:ConnectionId='3' Prefix="Блюда"                                                        />

            <SplitView x:ConnectionId='4' x:Name="splitView" PaneBackground="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                                                                                   
                   OpenPaneLength="220" CompactPaneLength="28" DisplayMode="CompactInline">
                <SplitView.Pane>
                    <controlsUWP:DockPanel>
                        <Grid x:ConnectionId='5'                                                                                                                          >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock x:ConnectionId='7' Text="Фильтры" x:Name="PaneHeader" Margin="60,12,0,0" Style="{StaticResource BaseTextBlockStyle}"/>

                            <TreeView x:ConnectionId='8' x:Name="TreeCategory" Grid.Row="1"                                               
                                                                            
                                      MinWidth="200">
                                <TreeView.ItemTemplate>
                                    <DataTemplate                                        >
                                        <TreeViewItem ItemsSource="{Binding Children}" Content="{Binding Name}" IsExpanded="True"/>
                                    </DataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                        <AppBarButton x:ConnectionId='6' x:Name="btnToogleFilterPanel" Icon="Forward" MaxWidth="20" MaxHeight="40" 
                                    VerticalAlignment="Center" VerticalContentAlignment="Center" 
                                    HorizontalContentAlignment="Center" Margin="0"
                                                                       />
                    </controlsUWP:DockPanel>
                </SplitView.Pane>
                <Grid x:ConnectionId='10'                                                                                        >
                    <controls:Section x:ConnectionId='11'                                                         
                              HeaderTemplate="{StaticResource ListHeaderTemplate}" 
                              HeaderButtonGlyph="&#xE2B4;"                                   
                                                                             >
                        <views:DishesList x:ConnectionId='12'                                         />
                    </controls:Section>
                </Grid>
            </SplitView>
        </Grid>
        <TextBlock Grid.Row="1">DishesView.xaml</TextBlock>
    </Grid>
</Page>

